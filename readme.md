# My jekyll blog

## Requirements:

- ruby
- bundler

### for ArchLinux:

```shell
sudo pacman -Syu
sudo pacman -S ruby ruby-bundler
```

## Run

```shell
bundle install --path vendor/bundle
bundle exec jekyll serve
```

